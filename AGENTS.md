# Mobile Prototype Agent Guide

## Prototype Instructions

In ChatGPT Work Mode, run `sites-preview start "$PWD"`, open `http://terminal.local:4173/` in the cloud browser, and verify the rendered app and its primary interactions. Keep that preview open and tell the user to inspect it in the cloud browser; do not present the local URL as a user-facing chat link. In Codex Desktop, run the local server yourself, open the preview in the in-app browser, and provide the clickable local URL. Do not deploy to Sites unless the user explicitly asks to share, publish, or deploy. Do not give the user server-start instructions when you can run it.

Before planning or implementing any mobile-app change, read this `AGENTS.md` in full. It is the source of truth for the template's runtime and component guidance.

Before making substantial visual changes, use the Product Design plugin's `get-context` skill when the visual source is unclear or no longer matches the current goal. When the user gives durable prototype-specific design feedback, preferences, or decisions, record them in `AGENTS.md`.

When implementing from a selected generated mock, treat that image as the source of truth for layout, component anatomy, density, spacing, color, typography, visible content, and hierarchy.

## Durable Design Decisions

- The next proposed Cizhou level-three redesign is a planning-stage direction named `一瓶见磁州`: keep one consistent white-slip black-painted meiping as the continuous visual anchor while its surrounding environment, light, sound, and knowledge lens progress from a cool open kiln yard, through a brighter northern market/transport setting, into a warm tavern use scene. The three knowledge questions are “how it was made,” “how a major northern folk-kiln system entered circulation,” and “how the meiping functioned in daily life.” This direction intentionally avoids three equally dark brown interiors. It is not yet authorized for runtime implementation; first produce and approve a three-scene visual board using one extracted transparent meiping master, three vessel-free backgrounds, separate contact shadows/foreground occlusion, and distinct sound beds. Do not attach yuhuchun-bottle carving facts or ceramic-pillow maker-mark facts directly to the meiping; retain those as later “more vessels” expansion content if this direction is selected. Full planning and asset requirements are in `design-audit/cizhou-meiping-one-shot-master-plan-v1-zh.md`.
- In the implemented one-meiping Cizhou scene, the fixed vessel must absorb each environment's light rather than remain a bright neutral cutout: use a cool desaturated gray response and light soft shadow in the kiln yard, subdued sun-warmed ochre in the market, and darker amber-brown absorption in the tavern. Keep the color response masked to the meiping and preserve the black ornament. The full-screen knowledge-hotspot layer must always use `pointer-events: none`; only the actual hotspot buttons may use `pointer-events: auto`, so empty space continues to reach the protected Carousel and horizontal scene dragging remains available.

- The selected Cizhou level-three knowledge direction is now `识窑 · 磁州三章`. Its primary navigation is three kiln characteristics, not three lifestyle categories: `黑白成画` (white-slip black painting, represented by a meiping), `刀笔生花` (carved/incised slip decoration, represented by a Northern Song yuhuchun bottle), and `瓷上人间` (folk painting, writing, and maker marks, represented by a Song Cizhou pillow). The vessel is evidence for the kiln characteristic; the reconstructed daily-use setting explains function; ambient sound supports atmosphere. Horizontal navigation switches the kiln characteristic, vessel, setting, hotspots, and sound together. The former downward cord-unsealing interaction is no longer the primary level-three interaction. The approved tavern raster at `public/assets/kilns/cizhou-life/cizhou-lifestyle-selected-v1.png` remains the style and composition anchor for the first chapter. For all three scenes, avoid detail accumulation. Art direction: soft-focus edges, restrained detail expression, prioritize large color fields, keep materials unified and clean, avoid stacking fine fragmented textures, and preserve an airy, premium overall finish.（避免细节堆砌。画面风格要求：柔焦边缘，克制的细节表达，大色块优先，材质统一干净，避免堆砌细碎纹理，整体通透高级。）
- The approved Cizhou level-two to level-three transition is a concise, caption-free buffered handoff. Let the red-hot ornament spread continue until the last instant with no completed-state hold, brightness flash, or cooling beat. Once spreading finishes, let the complete fired panorama, its depth, and residual color fade gently to the kiln's dark field over roughly 1.2 seconds with an ease-in-out curve. Then reveal the enlarged black-and-white floral area on the meiping over roughly 0.52 seconds, let that close-up settle briefly, and begin one slow pullback to the complete vessel and reconstructed setting. Do not add a second red-hot pattern image, intermediate title card, stage caption, or `轻触收束` prompt. Chapter copy, hotspots, audio, and navigation appear only after the pullback finishes. Reduced-motion users enter the ready state immediately.
- Keep the Cizhou handoff deliberately unhurried but visually singular: the fired view fades away before the meiping appears, and after the short meiping reveal and settle, only the camera scale changes across a roughly 5.7-second pullback. Preserve the accepted original pace through the first half (the first 3.6 seconds of the former `sine.inOut` path), then cover the remaining camera distance in roughly 2.1 seconds with a very light continuous acceleration. Match velocity at the midpoint so there is no visible gear change or late sprint. Do not add an animated tint, late shade change, or any other bridge motion.
- The Cizhou firing-to-knowledge handoff must read as one causal journey whose actions are ornament spreading, fired-view fade-out, meiping reveal, and meiping pullback. The ready state is chapter `01 黑白成画`, not a lifestyle-only landing page. Do not insert unrelated imagery, a new motif, a hard black cut, or enable horizontal chapter navigation before the pullback is fully settled.
- Once any kiln begins its heating-to-knowledge transition, keep the level-two identity, reading, vessel label, and orbit hint hidden through the entire knowledge state. They must not reappear for a frame when the knowledge layer mounts or while its first image is still fading in; restore them only when the user explicitly returns to the kiln sphere.
- The first level-three knowledge prototype is Cizhou kiln. A real flame tap supplies the screen-space ignition origin. From that point an irregular orange-hot front propagates slowly outward only through the panorama-aligned black ornament mask; reached ink remains in ember red, with no final whole-frame snap to brighter red at the end of the firing timeline. The fired color must read as internally lit, translucent incandescent red with restrained neutral pink-white hot cores, not as opaque vermilion paint or gold foil. Retain original ornament value where possible; use broad uneven heat pools without crisp yellow-gold contours or metallic-looking contrast. Do not hold, flash, cool, or add a glaze-sheen sweep after the propagation completes. The pale glaze receives only minimal local reflected warmth and must never become a full-screen red wash. At completion, fade the complete fired view evenly to the kiln's dark field before revealing the enlarged black-and-white meiping crop; do not use a top-to-bottom wipe, a sudden cut, or a separate popping peony/motif bridge animation. The accepted fired-state material is `public/assets/kilns/cizhou-knowledge/cizhou-panorama-fired-v2.png`, sampled only through the existing panorama-aligned ink mask; the former V1 ember overlay remains an archived fallback and must not be restored as the live flat-color treatment. Production assets live under `public/assets/kilns/cizhou-knowledge/`.
- The Jun level-three transition uses an in-place, saturation-driven kiln-heating direction. Do not propagate from the flame tap or screen center: the panorama and glaze texture remain spatially fixed while existing purple glaze regions heat simultaneously at their original positions. Derive temperature from the material itself—low-saturation purple stays in dark red-violet residual heat, medium saturation becomes ember red, and the most saturated detail reaches brighter red-orange with sparse warm-white hot cores and restrained neighboring kiln reflection. Preserve the milky sky-blue and moon-white majority; do not turn the entire screen red. Hold the completed heated state before the fire contracts and fades with the kiln view, then reveal the dark blue-violet knowledge page. The live target material is `public/assets/kilns/jun-knowledge/jun-panorama-kilnchange-v2.png`; V1 is only a fallback. Keep exact panorama mapping and glaze texture alignment, and avoid radial scans, neon color, metallic foil, paint-pour edges, or opaque flat recoloring. Production assets live under `public/assets/kilns/jun-knowledge/`.
- The accepted Yaozhou level-three direction is “glaze follows the knife, pooling into celadon.” Keep the carved panorama spatially fixed. During firing, derive the effect from carving depth rather than from a radial origin: shallow cuts retain thin yellow-green glaze, medium relief becomes warm olive jade, and medium-to-deep incisions rise into sustained orange-red heat with sparse warm-white cores. Do not cool the carving back to green before navigation: keep the red-hot grooves, orange furnace edge reflection, full-screen heat distortion, sparks, and flame alive until the final page cut, then fade them together. Preserve glaze texture and bubbles, use only thin jade-white ridge light, and avoid luminous line art, electric tracing, a glossy sweep bar, or an overall flat red wash. The live runtime assets are the seam-repaired `public/assets/kilns/yaozhou-knowledge/yaozhou-panorama-glaze-depth-v2.png` and its aligned `yaozhou-carving-depth-mask-v2.png`; V1 remains a fallback. The firing timeline is 4.6 seconds.
- The accepted Ge level-three direction is “gold thread and iron wire under furnace heat.” Keep the thick milky glaze and panorama spatially fixed, but make the whole secondary scene unmistakably kiln-fired through orange-red reflection, full-screen heat distortion, sparks, and a white-hot flame. Reveal the existing crack network in two material-driven stages rather than from a radial origin: broad iron-wire fissures become deep ember red first, then fine gold-thread fissures interweave in brighter orange-red with sparse warm-white hot cores. Preserve the two crackle hierarchies and glaze texture; avoid lightning tracing, shattered-glass displacement, metallic gold foil, opaque paint-like recoloring, or a cooling reversal. Hold the completed incandescent double crackle visibly, then let a central coarse fissure deepen while the camera eases into it and the kiln view and fire fade together. The live aligned mask is `public/assets/kilns/ge-knowledge/ge-crackle-dual-mask-v1.png` (red channel iron wire, green channel gold thread), generated by `scripts/generate-ge-crackle-mask.py`. The firing timeline is 4.8 seconds.
- The accepted Guan level-three direction is “fire held beneath thick glaze, ice crackle floating above it.” Keep the gray-blue/powder-celadon panorama spatially fixed. Do not repeat Ge's glowing double line network: the major ice-crackle boundaries must read as single, fine, continuous dark-wine/black contours with only a restrained purple presence—not thick bands, fragmented dashes, or glowing tubes—while the interiors reveal orange-red underglaze furnace light according to glaze thickness. The underglaze warmth must rise broadly and evenly, with only subtle thickness modulation; never form localized red heat islands or blotches. The boundary must not collapse to neutral black, but the violet must remain subordinate to the furnace red. Remove isolated dark pits and specks from the crackle mask without fragmenting real cracks: use strong crack segments as anchors, reconnect the weaker adjacent signal, then reduce the connected band back to its thin centerline so every major contour stays complete without becoming wider. Thin glaze reads hotter and more transparent; thick milky glaze preserves its jade-blue body while showing restrained warm-white pearly bubble cores. Hold the completed underfire state without cooling, then magnify one central translucent glaze plate like a lens until it fills the view and crossfades to knowledge. The live aligned mask is `public/assets/kilns/guan-knowledge/guan-firing-mask-v1.png` (red channel broad ice-crackle boundary, green channel glaze-thickness proxy, blue channel pearly bubble cue), generated by `scripts/generate-guan-firing-mask.py`. The firing timeline is 5.1 seconds.
- The accepted Longquan level-three direction is continuous fire transmitted through the original celadon glaze, not white-hot cells or any synthetic honeycomb. Keep `public/assets/kilns/longquan-panorama-v2.png` spatially fixed and use only its real luminance, celadon body, carved relief, fine crackle, and surface texture to drive the material response. The archived procedural cell mask must not participate in the live runtime. Heat rises beneath the glaze from deep ember through orange to restrained near-white incandescence, while a translucent celadon veil and the source material's high-frequency relief remain visible above it so the result reads as light glowing from within thick glaze rather than flat recoloring. Use broad continuous transmission with subtle material-derived variation; never introduce invented partitions, polygon cells, honeycomb geometry, bright green dividers, painted blocks, beige wash, or a separate crack-line overlay. The idle phase must remain pixel-faithful to the original panorama. Hold the final internal white heat without cooling, and let the flame, kiln view, heat distortion, and sparks fade together into the knowledge page. The firing timeline is 5.2 seconds.
- The accepted Ding level-three direction is “impression receives fire; ivory glaze condenses light.” Keep the live `public/assets/kilns/ding-panorama-v3.png` fixed and derive every animated response from its existing peony, lotus, leaf, and scroll relief; never invent or redraw ornament. Deep impressed and incised recesses receive ember heat first, then fill with translucent orange molten-glaze light, while only the deepest glaze reservoirs reach restrained ivory-white hot cores. Raised edges remain warm ivory and catch a pearly reflected highlight, so the result reads as fire stored beneath white glaze rather than lava painted on top. Fill follows material depth, not the tap point or a radial scan, and must remain soft-edged and viscous without neon outlines, geological cracks, gold foil, or a full-screen red/orange wash. Hold the completed hot imprint before the flame, panorama, sparks, and kiln atmosphere fade together into the Ding knowledge page. The live aligned mask is `public/assets/kilns/ding-knowledge/ding-impression-depth-mask-v1.png` (red recess depth, green raised ivory ridge, blue deepest glaze reservoir), generated from the live panorama by `scripts/generate-ding-impression-mask.py`. The firing timeline is 5.0 seconds.
- All kiln secondary scenes must read as active furnace-firing environments. Keep the shared language of heat refraction, sparks, firelight, and sustained incandescent material, but differentiate each kiln by which material feature reacts and how: Cizhou black ornament propagates red from the ignition point, Jun saturated purple glaze heats in place by saturation tier, Yaozhou carved depth and pooled glaze carry the heat, Ge double crackle heats in two line hierarchies, Guan transmits furnace red through large thick-glaze plates while the ice-crackle boundaries stay dark, Longquan transmits continuous internal heat through its original celadon body and relief, and Ding lets its authentic impressed recesses receive molten-glaze fire while raised ivory edges remain pale. Do not create eight unrelated transition styles, and do not flatten them into one identical full-screen red treatment.
- The main experience is a mobile-first, aesthetic-led Song-ceramics exploration prototype.
- The app opens on an otherwise pure-black “器之初·定心开器” screen. It begins directly with the processed opened-clay GLB in a small, near-top view; `public/assets/kilns/origin-clay-v1.png` is archived source material and must not be preloaded or rendered in the live intro. The opening expands into the spiral-cavity transition and reveals the Cizhou sphere. Preserve the absence of intro copy, borders, logos and visible navigation controls; initial focus may make the clay trigger keyboard-accessible, but must not create visible UI.
- The live intro sequence is: the processed opened-clay GLB slowly rotates as the stable 3D master, click accelerates the spin and continuously enlarges the same top opening, the cavity spiral takes over inside that opening, then the intro veil and Cizhou sphere crossfade on the same entering timeline. Do not use the old raw-clay image or the two generated hand layers in the runtime; they remain archived source material only. The GLB processing recipe is `scripts/process-intro-glb.mjs`, the runtime model is `public/assets/kilns/intro-3d/opened-clay.glb`, and the accepted cavity transition remains in `public/assets/kilns/intro-v2/`.
- The generated MP4 is archived source material and must not participate in the live intro. Keep the GLB loading interval pure black, reveal only the real 3D model once ready, animate the existing cavity-spiral raster as the line-work bridge, and hand off directly to the already-rendered Cizhou spherical panorama so the final transition frame and the secondary home background are the same visual source.
- Keep the click-to-entry camera push as one uninterrupted, perceptually linear zoom across the acceleration and entering phases; do not restart easing at the phase boundary. Let the Cizhou panorama settle before its fire portal eases in with delayed opacity and blur recovery, and keep the invisible intro veil mounted long enough for that fire reveal to finish without a snap.
- Preserve the identity of one continuous clay body through the intro: the GLB remains the visible master until the camera is deep inside its opening, the cavity raster emerges late and only through a soft-edged mask centered on that same opening, and the Cizhou panorama must not begin taking over until the model has nearly filled the viewport. Avoid an early full-screen circular image that reads as a second piece of clay.
- The `?introValidation=rotating` experiment that rotated the existing single `clay-opened.webp` image has been rejected and is no longer the implementation path. Do not restore flat-image rotation. The processed GLB is the default intro model, with the existing cavity-spiral transition as the final handoff into Cizhou. The source-material plan and prompts remain in `design-audit/rotating-clay-generation-plan-v2-zh.md`.
- Navigation is an eight-position boundless spherical kiln world, not a linear corridor or conventional carousel.
- Dragging in any direction rotates the kiln sphere; release adds restrained inertia and comes to rest on the explored background detail. The nearest kiln identity may update for context, but only keyboard navigation explicitly moves the view to a kiln center.
- The kiln atmosphere is the first visual subject. Its glaze color, texture, light, and motion change before the representative vessel becomes legible.
- Representative vessels use their original glaze colors whenever a reliable source image is available. They remain complete, independently legible, suspended, and tappable.
- Each kiln has its own historically grounded atmosphere while the eight environments remain part of one continuous, seamless ceramic sphere.
- The Ge kiln world uses thick milky gray-celadon/rice-yellow glaze, interwoven dark and warm-gold crackle, and subtle pearl-like glaze bubbles.
- Kiln backgrounds are clean 2:1 equirectangular ceramic environments with no fire, portal, vessel, or focal object baked into them. Fire portals belong to a separate interactive foreground layer.
- Background motion preserves physical ornament: Ge crackle and Cizhou carving remain fixed while raking light moves; Jun glaze may drift only very slightly.
- Do not render a persistent bottom-right kiln locator. The sphere should rely on direct exploration, the current kiln identity, and keyboard navigation rather than a visible circular or constellation control.
- Use the supplied transparent ceramic PNGs as the foreground objects. Do not redraw them in CSS or SVG.
- Returning to the main experience should preserve the current spherical kiln position.
- Ge, Cizhou, and Jun use an independent fire-first entrance state: a restrained live fire portal appears before the representative vessel, and tapping it transitions into the original-color vessel layer. Fire motion should be implemented from the supplied raster assets with subtle layered transforms and filters, with distinct pacing per kiln and a reduced-motion fallback. Ge keeps its luminance pulse especially subdued so entering it directly from the ignition screen does not create an obvious brightness beat.
- The remaining five kilns (Ru, Guan, Longquan, Yaozhou, Ding) use the same environment-first entrance hierarchy with their supplied dedicated 2:1 panoramas and black-background fire rasters. Their runtime assets live in `public/assets/kilns/` and `public/assets/kilns/fire-portals/`; do not restore the former material portal placeholders or reuse another kiln's fire. The original generation contract and prompts remain archived in `design-audit/remaining-five-kiln-portal-material-plan-v1-zh.md`.
- Ru, Guan, and Longquan use their supplied panorama V2 assets (`ru-panorama-v2.png`, `guan-panorama-v2.png`, and `longquan-panorama-v2.png`), while Ding uses the more detailed live `ding-panorama-v3.png`. Former versions remain archived as non-runtime fallbacks. Yaozhou panorama V1 is still provisional because the user finds it too generic and reports a visible background bug; do not mark its redesign complete until a V2 asset is supplied and verified. Yaozhou fire uses `yaozhou-fire-v2.png`: keep it as one compact, connected, asymmetric flame with carved-knife flow, and do not restore the scattered V1 silhouette.
- Fire portals must read as continuously burning rather than as breathing raster cards: preserve each supplied flame silhouette and kiln-specific palette, but animate internal upward flow, irregular tongue deformation, asynchronous edge turbulence, and localized brightness pulses in real time. Keep whole-portal floating extremely restrained and retain a static fallback for reduced motion or unavailable WebGL.
- Treat the current Cizhou fire motion as the quality and pacing benchmark for the three-kiln demo. Preserve it unless the user explicitly requests a new Cizhou direction.
- The first kiln-atmosphere sample set is Ru (quiet), Guan (balanced), and Yaozhou (slightly active). The atmosphere must cover the complete phone interface rather than cluster around the central fire: heat refraction travels across the full background and sparks rise sparsely from distributed positions across the screen. Keep UI text sharp above the atmosphere, make the effect noticeable within 2-3 seconds, reduce it during sphere interaction, and disable refraction and sparks for reduced motion.
- The accepted cross-kiln strength baseline is Guan for spark density/frequency and Yaozhou for full-screen heat refraction. Sparks should use kiln-coordinated ash-gold and muted copper tails with a tiny pale hot core; avoid saturated orange dots that resemble fireflies or UI indicators.
- Full-screen sparks must be seeded and regenerated across upper, middle, and lower vertical bands; do not let the visible distribution settle only in the lower half. Upper sparks remain below the text layer and may be slightly sparser through randomized lifetimes.
- After reviewing the full-screen distribution, the user requested a small density increase: use a 12-spark cap with a roughly 0.47-0.86 second generation interval across Ru, Guan, and Yaozhou while preserving the accepted palette and three-band distribution.
- The full-screen kiln atmosphere now applies to all eight kilns. Use quiet motion for Ru and Ding; balanced motion for Ge, Guan, and Longquan; and slightly sharper motion for Cizhou, Jun, and Yaozhou. Strength remains unified at the accepted 12-spark/Guan-density and Yaozhou-heat baselines.
- Give Ge fire clearer internal rise, edge movement, and luminance variation while keeping its overall character restrained and quiet.
- Bring Jun fire closer to Cizhou's continuous-burning quality while preserving Jun's celadon, blue-violet, and copper-red palette. It should form rising, separating tongues with irregular pulses, not read as a smooth breathing droplet.
- Keep Jun's irregular luminance pulse subdued; its kiln identity should come primarily from color mixing and rising tongue motion rather than conspicuous brightness flashing.
- Ding's knowledge transition is an aligned, full-field material change from the cold `ding-panorama-v3.png` into the generated `ding-panorama-fired-v2.png`. Treat the fired panorama as the authoritative kiln-light and glaze response; blend the complete ceramic field at a constant linear rate through the first 86% of the five-second transition, then hold the fully fired frame briefly before the shared fade. Do not reintroduce staggered region timing, smoothstep acceleration, or low-frequency timing offsets. The former `ding-impression-depth-mask-v1.png` is archived provenance only and must not drive runtime color. Avoid procedural red overlays, crisp glowing mask boundaries, broad screen-blend halos, uniform CSS color washes, or invented lava cracks; the result must read as one ceramic surface reaching peak firing temperature, never a red graphic pasted above it.
- Free sphere exploration must not automatically snap or recenter after a drag. Let the view coast with smooth, near-linear deceleration and come to rest on any background detail; keyboard navigation remains the explicit assisted way to move between kiln centers.
- Keep sphere travel continuous in both axes so users can explore past the former vertical limits. Kiln identity may follow the nearest viewing direction, but it must not pull the camera back to that kiln.

- The accepted Ru level-three direction is “clouds open to transmit fire; glaze light condenses.” Use the sphere-safe cold panorama `public/assets/kilns/ru-panorama-v4.png`, which removes the rejected V3 center-star convergence, as the live Ru environment. Its aligned fired state is `public/assets/kilns/ru-knowledge/ru-panorama-fired-v1.png`. Keep cloud bands, sparse hairline crackle, and panorama mapping spatially fixed; reveal the fired material broadly from the glaze itself, retain a visible cool sky-blue veil over thick areas, let only thin cloud regions reach restrained orange and soft ivory heat, hold the completed fired state, then fade the kiln view into the Ru knowledge page. Never restore a radial origin, center spotlight, pole pinching, uniform orange wash, painted red overlay, lava fissures, or bright crack outlines.

- All eight level-three kiln transitions preserve their established spatial choreography and kiln-specific material behavior, but their shared firing timeline must now use linear timing for the panorama, depth, atmosphere, and fire hold. The visible fire stack fades continuously from full opacity to transparent across the same duration as its kiln background animation; never hold the flame fully opaque until a sudden late drop, and never remove it on a shorter generic 920ms portal transition. Keep each kiln's existing flame contraction, internal flow, color, and position underneath this synchronized opacity envelope. Reduced-motion behavior remains immediate and must not be weakened.
- The accepted Cizhou level-three scene UI is deliberately sparse: keep the title, one-line trait thesis, and representative vessel evidence; omit the redundant chapter kicker, soundscape caption, labeled back/audio controls, and bottom scene navigation. Back and audio remain accessible icon-only controls positioned below the device top safe area and above the bottom safe area. Horizontal swipes should read as moving between lived scenes, using lightweight parallax/opacity presentation while scrolling and a restrained GSAP settle to the nearest scene only after Carousel momentum ends. Commit the active trait and crossfade its audio only after that settle; do not restore React state updates on every scroll frame, CSS scroll snapping, or edits to the protected Carousel runtime.

- The current Cizhou one-meiping carousel treatment has been rejected as a transition model. Do not keep tuning it as three full-frame images that slide horizontally behind a fixed cutout. The next approved planning direction is a fixed-camera in-frame environmental transformation: one unchanged meiping silhouette and contact point, three backgrounds rebuilt to the same lens/horizon/support-plane geometry, aligned scene-specific support/contact/occlusion layers, and gesture progress driving stacked cross-dissolves, local atmospheric bridges, vessel lighting, and audio rather than visible page translation. The market support-plane mismatch and the kiln-yard floating vessel are explicit failures to remove. Preserve the sparse, soft-focus, large-color-field art direction. The detailed execution specification is in `design-audit/cizhou-transition-rethink-v2/transition-rethink-v2-zh.md`. This direction is approved for planning only until aligned gray-box compositions are reviewed.
- The user subsequently approved executing the Cizhou lived-scene sequence as `出匣初明 → 车辙北行 → 酒肆入席`. The accepted clean second- and third-scene assets contain the same locked meiping inside the full-frame image: `02-northbound-cart-integrated-v5-clean-r5.png` and `03-tavern-meiping-integrated-v4-clean-r5.png`. Runtime keeps the independent meiping stage hidden so the scenes never show duplicate or misregistered bottles; preserve that rule. Keep the cart's low straw sleeve around the bottle base and the tavern's single small wine cup and two restrained distant diners embedded in the background. The tavern image now contains its own tight bottle-foot contact shadow and warm ambient occlusion; do not restore the former `.cizhou-scene-layer[data-scene-layer="tavern"]::after` CSS shadow or add another floating overlay. `02-northbound-cart-aligned-v3.png` and `03-tavern-table-aligned-v3.png` are source environments, not current runtime scenes.
- Preserve full scene clarity in the Cizhou lived-scene sequence. Do not apply Gaussian blur to `.cizhou-scene-layer` or enlarge the completed full-frame scene assets to disguise transitions. Scene integration must come from locked geometry, contact shadows, occlusion, ambient color/light absorption, and opacity cross-dissolves; keep all settled scene layers at `blur(0)` and scale `1`.
- `01-kiln-yard-meiping-integrated-v4-clean-r4.png` is the accepted first-scene runtime asset. It preserves the grounded meiping and contact shadow while making the vessel absorb the cool blue-gray kiln-yard light and reducing dirty wall/table microtexture into clean broad color fields. Together with `02-northbound-cart-integrated-v5-clean-r5.png` and `03-tavern-meiping-integrated-v4-clean-r5.png`, all three settled scenes now follow the approved soft-edge, restrained-detail, broad-color-field, clean-material direction. Keep `.cizhou-meiping-stage` hidden throughout the arrival and lived-scene sequence; do not restore its reveal, create a duplicate vessel, or revert to the noisier legacy assets.
- The approved Cizhou settled-scene micro-motion is deliberately ambient rather than theatrical, but it must still be visibly recognizable within a two-to-three-second observation: the kiln yard uses a localized distant-lantern halo disturbed by a thin lateral air veil, the northbound cart uses low road-bound dust drifting in the direction of travel, and the tavern uses a restrained candle flicker with a faint room-light response. Keep motion localized away from the vessel's identity-defining pattern, run it only for visible scene weights, animate compositor-friendly opacity/transforms, and leave static restrained states under `prefers-reduced-motion`. Do not introduce camera shake, floating bottle motion, full-screen fog, heavy particle showers, or conspicuous brightness pulsing.
- Cizhou scene settling must not pause after the finger/momentum stops and then restart from zero velocity. Keep the post-scroll settle delay near one frame (currently 34ms), use a compositor-friendly monotonic deceleration (`power1.out`) rather than `power2.inOut`, and keep the settled duration short and travel-dependent. Do not edit the protected generic Carousel physics to tune this app-specific scene settle.
- The Cizhou level-two-to-level-three pullback must behave as one camera move, not a bottle cutout scaling over a static scene. Animate a shared arrival-camera composite containing the aligned background/support plane, complete meiping group, contact shadow, scene light, and foreground haze; keep the meiping stage at a constant scale within that composite. Use the peony area around the vessel's mid-body as the close-up focus, then pull the complete camera composite back to the grounded final view. The transition must finish by clearing GSAP inline filter/transform/opacity styles so scene-specific absorption remains active. Do not restore a timer that forces the ready state ahead of the timeline, and never animate only `.cizhou-meiping-master` or `.cizhou-meiping-stage` as the primary pullback target.

## Editing Boundary

- Build app-specific UI in `src/Prototype.tsx` and `src/prototype.css`.
- Treat `src/App.tsx`, `src/main.tsx`, `src/styles.css`, `src/mobile/`, `public/assets/iphone/`, `public/assets/android/`, `public/assets/status/`, `vite.config.ts`, `worker/index.js`, and `scripts/prepare-sites-build.mjs` as protected runtime files. Do not edit, replace, remove, or recreate them unless the user explicitly asks to change the mobile runtime itself. For an explicit runtime change, update the affected lock hashes only after verifying the new runtime behavior.
- Run `npm run check:runtime` before preview or handoff. If it fails, restore the protected runtime instead of weakening or bypassing the check.
- `npm run build` preserves the mobile runtime and prepares the static Cloudflare Worker output required by Sites. Before a Sites handoff, confirm `dist/client/index.html`, `dist/server/index.js`, `dist/.openai/hosting.json`, and source `.openai/hosting.json` exist, then run `npm run test:sites`. Do not replace this project with a Vinext starter.

## Runtime Contract

- Preserve the mobile device runtime unless the user's task explicitly asks otherwise. Do not replace it with a standalone page. Visual fidelity applies to app-owned content inside the device screen, not to template-owned device chrome.
- Keep `App` composed around `PhoneFrame` -> `KeyboardProvider`, with `StatusBar`, app content, `HomeIndicator`, and `KeyboardDock` mounted inside the phone frame. `StatusBar` and the iOS home indicator are overlaid device chrome. When the Android keyboard is closed, the app viewport reserves the protected navigation-bar region instead of painting behind it. When the Android keyboard is open, preserve the current full-screen keyboard layout: its asset includes the IME navigation strip and the separate black navigation bar is hidden. iOS screens continue to paint behind the home-indicator area and own their safe-area content padding.
- Preserve the `iPhone` / `Pixel 10` device picker and both calibrated device presets. The Pixel screen is `427 x 952`; its `32 x 32` camera circle and `public/assets/android/navigation-bar.svg` bottom navigation bar are protected device chrome, not app content.
- Preserve the device picker's intentionally lightweight Codex styling in the top-right corner: its trigger wrapper is borderless and transparent, its trigger sizes to content, and its right-aligned menu uses the compact 3px inset plus the specified hairline and elevation shadow layers. Keep the prototype root and default app screen white.
- Preserve `StatusBar` as live device chrome, including its platform-specific typography, source status-icon assets, and spacing. Pixel 10 uses Roboto, Android indicators, and 32px top, left, and right padding. iPhone uses its iOS indicators, system typography, and calibrated spacing. Do not hardcode screenshot times like `9:41` into the status bar, replace its real-time clock, or move status bar content into app markup unless the user explicitly asks for a fixed/mock device time.
- `PhoneFrame` owns the calibrated device frame, screen portal, device picker, camera cutout, and custom cursor. Keep device assets in `public/assets/iphone/` and `public/assets/android/`; if an asset fails to load, repair the asset path or restore the asset instead of removing the frame, keyboard, or image render.
- Use `MobileScroll` directly for simple single-screen prototypes. Use `FlowStack` for conventional multi-screen flows whose routes can own their fixed header and footer; when using it, define each route as a `FlowScreen`: `{ id, header?, headerHeight?, footer?, footerHeight?, render }`, and use `flow.push(screen)`, `flow.pop()`, and `flow.replace(screen)` from `FlowStack` render callbacks or `useFlow()` instead of introducing another router.
- Use `Carousel` for a carousel, horizontal rail, swipeable cards, image or media strip, horizontally scrollable cards, chip rail, or other horizontal collection.
- For a layered app shell—such as a persistent composer, independently presented sheet, pushed/peek sidebar, or app-wide transition—compose directly in `Prototype.tsx` rather than forcing it through `FlowStack`. Keep app-owned fixed chrome as sibling layers outside `MobileScroll`.
- When using `FlowScreen`, put route-owned fixed headers or footers in `FlowScreen.header` or `FlowScreen.footer`. Set `headerHeight` to the visible app-toolbar height; `FlowStack` adds the device's top safe-area/status-bar inset automatically. Do not include `StatusBar` or its height in the header. Set `footerHeight` to the full app-footer height. `FlowScreen.footer` is an overlay, not reserved layout space; screens using it must add their own bottom content padding such as `padding-bottom: calc(var(--flow-footer-height) + var(--mobile-safe-area-height) + 24px)` so final content can scroll above the footer while still painting behind it.
- Render only scrollable content inside `MobileScroll`; it is for content that should move with scroll and rubber-band overscroll. Keep app-owned headers, nav bars, tabs, composers, and overlays outside it. This keeps scroll physics, safe areas, keyboard insets, scrollbars, and drag click suppression active without letting content paint under fixed chrome.
- Buttons, links, cards, and images inside `MobileScroll` should still allow drag scrolling when the pointer moves beyond tap slop. Use `data-scroll-drag="ignore"` only for rare controls that must own the drag gesture themselves.
- Do not add `var(--keyboard-height)` to ordinary screen/content padding inside `MobileScroll`; the scroll viewport already shrinks above the simulated keyboard. For custom fixed composers, search bars, or toast chrome, use `useKeyboardInsets().bottomInset`. It is relative to the app viewport: Android returns `0` while the closed-keyboard viewport already reserves navigation, then returns the keyboard height while open; iOS continues to clear the home indicator while closed and ride directly above the keyboard while open. Do not pin custom bottom chrome to `bottom: 0` or only `keyboardHeight`.
- Use `KeyboardInput`, `KeyboardTextarea`, or `MobileTextField` for every text-entry control. A raw `input` or `textarea` disconnects focus, keyboard animation, safe-area insets, and attached surfaces.
- Use `BottomSheet` for phone-scoped sheets. Its props are `open`, `onOpenChange`, `title`, optional `description`, optional `snap`, and `children`; it renders through the phone screen portal and dismisses the keyboard before opening.

## Horizontal Carousels

- Use `Carousel` for horizontally draggable cards, images, media, chips, or other horizontal collections. Do not recreate these with `overflow-x`, custom pointer handlers, or a generic div.
- `Carousel` can be nested directly inside `MobileScroll`. It owns horizontal gestures and automatically yields vertical gestures to the parent.
- Never put `data-scroll-drag="ignore"` on or around a `Carousel`; doing so prevents vertical parent scrolling when a gesture begins inside it.
- Do not add CSS scroll snapping to `Carousel`; its runtime owns momentum and release motion.
- Use `data-scroll-drag="ignore"` only when a control must prevent parent scrolling in every drag direction.

See `src/mobile/COMPONENTS.md` for the full component and gesture contract.

## Keyboard Rule

The simulated keyboard is a separate top-layer component. Before presenting anything that behaves like iOS navigation or modal UI, dismiss it first.

Call `keyboard.hide()` before:

- pushing, popping, or replacing FlowStack routes
- opening bottom sheets, action sheets, dialogs, menus, or navigation sheets
- starting transitions where the destination should not inherit text-input focus

`FlowStack` already hides the keyboard for `push`, `pop`, and `replace`. `BottomSheet` already hides it before opening. If you add new modal/sheet/navigation primitives, follow the same rule.

When a composer, search surface, or other keyboard-attached component closes, call `keyboard.hide()` in the same event before changing that component's open state. Position attached surfaces from `useKeyboardInsets()` rather than a separate timer or visibility flag so both dismiss together.

When any text-entry control loses focus, dismiss the simulated keyboard. If the control is custom or does not use the runtime's keyboard-aware fields, handle its blur event and call `keyboard.hide()` explicitly. Keep the keyboard open only when focus is moving directly to another text-entry control that should share the same keyboard session.

## Interaction Rules

- Do not trigger buttons or inputs after a pointer has become a drag. Preserve the drag suppression behavior in `MobileScroll`.
- Do not allow native browser image/file dragging inside the phone frame. Preserve the phone-level `dragstart` suppression and non-draggable image styles so scroll drags that begin on images still scroll the prototype.
- Use `KeyboardInput`, `KeyboardTextarea`, or `MobileTextField` for text entry so the simulated keyboard and safe-area insets stay connected.
- Fixed phone chrome should not animate with pushed screens. Screen content can animate; the status bar, camera cutout, and preview chrome should stay put.
- Keep the keyboard below the home indicator/safe area layer in z-index, and above ordinary app UI while visible.
- Keep the home indicator as the topmost safe-area layer in the z-index above everything else in the prototype.
