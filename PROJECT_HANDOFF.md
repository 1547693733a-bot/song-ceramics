# 宋代八大名窑科普小程序｜项目接管说明

## 当前有效基线｜2026-08-04

- 当前接管任务为 `019fcced-d13a-7e70-97c3-c836cb0bb93e`；来源任务与当前任务共用同一工作目录，因此本次是接管和整理，不是复制第二份代码。
- 当前项目目录就是 `D:/vbcoding/宋瓷/1/kiln-wheel-demo`；来源任务与当前任务共用同一工作目录，不需要再复制或合并第二份代码。
- 当前首屏以无手版 `原始泥团 → 开孔泥坯 GLB 慢速旋转 → 点击加速并推进入孔 → 孔内旋纹转场 → 磁州窑球面` 为准，源码状态机是 `idle → accelerating → entering → hidden`；GLB 加载失败时回退到 `public/assets/kilns/intro-v2/clay-opened.webp`。
- 当前球面拖拽释放后只做克制惯性并更新最近窑口身份，不自动吸附或回中；方向键才执行明确导航。
- 当前可继续工作的入口是 `src/App.tsx` → `src/Prototype.tsx`；视觉样式在 `src/prototype.css`，移动端运行时在 `src/mobile/`。
- 关键资产与处理脚本分别是 `public/assets/kilns/intro-3d/opened-clay.glb` 和 `scripts/process-intro-glb.mjs`；孔内转场仍使用 `public/assets/kilns/intro-v2/cavity-spiral-transition.webp`。
- 已核对：`npm.cmd run check:runtime`、`npm.cmd run build`、`npm.cmd run test:sites` 通过；`npm.cmd run test:runtime` 的 8 项测试尚未进入断言，原因是本机缺少 Playwright Chromium 可执行文件。

下文中带有“轻捏”“双手定心”“最近窑口吸附”等内容的段落属于历史方案或历史记录；若与源码冲突，以本节和当前源码为准。

## 项目目标

这是一个面向手机端、未来计划落到微信小程序的宋代八大名窑科普体验。产品以审美与氛围为先、信息功能为后：用户不是从列表进入窑口，而是在一个连续、无边界的球形“窑境”中游历。

核心交互设定：

- 用户可以向任意方向拖动球面，松手后带轻微惯性；最终确认的规则是不自动吸附或居中到窑口，窑口身份只随最近视线方向更新，键盘方向键才是显式的窑口导航。
- 八个窑口分布在同一个球形空间中：哥窑、汝窑、官窑、磁州窑、龙泉窑、钧窑、耀州窑、定窑。
- 窑口气氛先于器物出现；釉色、纹理、光线与运动先建立身份。
- 入口最终应使用与各窑烧造气氛对应的独立火焰，而不是让器物承担入口功能。
- 进入火焰后，原色器物才显现，并承载进一步阅读或探索。
- 当前优先完成哥窑、磁州窑、钧窑三个 Demo，其余五窑暂不继续深化。

## 已完成

- 八窑球面坐标、窑口定位与八节点定位器。
- 横向、纵向拖拽，松手惯性与最近窑口身份更新（不自动吸附）。
- 键盘方向键导航与 `Escape` 退出器物阅读状态。
- 哥窑、磁州窑、钧窑三套真实全景纹理接入 WebGL 等距柱状球面采样。
- 三窑差异化背景运动：哥窑与磁州窑保持纹理稳定，仅改变掠射光；钧窑只做极轻微、低速流动。
- 八件器物素材与点击阅读层；哥窑、磁州窑、钧窑已使用原色透明图。
- 哥窑、磁州窑、钧窑三张独立纯黑底火焰素材已归档到 `public/assets/kilns/fire-portals/`。
- 三窑已接入“火焰入口 → 推进熄入 → 原色器物显现 → 点击阅读”的入口状态。
- 火焰当前由主体层与柔化余焰层组成，使用滤色混合、错位、呼吸、漂浮和分窑节奏，并提供减少动态偏好回退。
- iPhone / Pixel 10 双设备预览框架与移动端安全区、状态栏、键盘运行时。
- Cloudflare Sites 构建产物与 Worker 回退测试。

## 当前实现状态

主实现位于：

- `src/Prototype.tsx`：八窑数据、球面 WebGL、拖拽/惯性/吸附、器物阅读交互。
- `src/prototype.css`：窑境、器物、标题、阅读层与八节点定位器的视觉样式。
- `public/assets/kilns/`：哥窑、磁州窑、钧窑全景图与其余五窑共用图集。
- `public/assets/kilns/fire-portals/`：三窑独立火焰入口素材及磁州窑备选版本。
- `public/assets/ceramics/`：八件器物透明图。
- `output/chatgpt-web/`：历次生成的三窑整屏视觉稿。

当前代码已经完成火焰优先入口，并已将火焰主体升级为实时 WebGL 渲染：原始素材提供轮廓和窑口配色，着色器负责持续上升的内部热流、上部火舌形变、异步边缘湍流和局部亮度脉冲。哥窑、磁州窑、钧窑分别使用偏稳、迅疾、黏稠缓慢的燃烧参数；WebGL 不可用或用户偏好减少动态时回退到静态素材。

当前有效的球面导航规则：拖拽释放后只做克制惯性减速，不再自动吸附或回中；`activeIndex` 仍会根据最近视线方向更新，用于窑口身份和入口状态，不代表镜头被拉回该窑口。方向键通过 `navigateTo()` 才会将视角动画导航到目标窑口。

## 三窑火焰方向

- 哥窑：克制、安静的灰青裂焰；灰青外焰、细金热流与铁色暗隙，避免直接把开片纹样画成图标。
- 磁州窑：刀削般连贯的黑白剔焰，强调白地黑彩与果断刀笔，不只是普通橙色火焰换色。
- 钧窑：天青乳浊、蓝紫与铜红彼此渗化的窑变焰，运动黏稠、缓慢，避免稳定紫色霓虹。

实际入口使用 `public/assets/kilns/fire-portals/` 下的独立纯黑底 PNG，通过滤色混合叠加到球面背景。`output/chatgpt-web/*-kiln-spherical-fire-v2.png` 仍只是包含背景与文字的整屏概念稿，不参与运行时入口渲染。

## 历史阶段的下一步建议（旧版轻捏首屏）

1. 由用户在本地预览中确认两次轻捏的主观速度；当前时序以动作清晰可读为优先。
2. 复测自由拖拽误触、窑火点击推进、器物显现和返回后球面位置保持。
3. 检查低性能设备上的帧率、黑底滤除、边缘锯齿与 WebGL 静态回退。
4. 三窑 Demo 定稿后，再决定是否为其余五窑生产独立全景与火焰素材。

## 2026-08-02 最新接管停点

来源任务：`019fc1bb-7996-72a0-986f-99c31a167189`。

上一任务已经收到并确认三张方形素材，但在准备去除绿幕并接入动画时停止。该停点现已继续完成：

- `assets-source/intro/hand-open-green.png`：年轻女性二维动画手，张开／松开状态，纯绿背景。
- `assets-source/intro/hand-pinch-green.png`：同一只手的轻柔捏合状态，纯绿背景。
- `assets-source/intro/clay-pinched-black.png`：写实泥核受压状态，纯黑背景。

以上文件已从 `D:/vbcoding/宋瓷/` 复制进项目，原文件保留不动；`assets-source/` 是源素材区，不直接参与当前构建。

已经确定的视觉与交互方向：

- 保留真实泥核与二维赛璐璐手之间的材质冲突，不改回真人手。
- 点击泥核后做两次短促、轻柔的捏合，不做抓握、用力挤压或夸张漫画符号。
- 手退出后再执行现有泥层扩散，避免动画层次同时发生而显得混乱。
- 启动动画结束后默认进入磁州窑，而不是当前代码中的哥窑。
- 原始泥核、八窑球面结构、三窑实时窑火和受保护的移动端运行时全部保留。

当前实现已经：

- 将三张运行时素材复制到 `public/assets/kilns/intro/`，源素材仍保留在 `assets-source/intro/`。
- 使用运行时色键渲染清除两张手图的绿幕，保留原始线条和赛璐璐色彩。
- 通过同一个 React 手势状态同步切换手部与泥核，完成“张开 → 第一次轻捏 → 松开 → 第二次轻捏 → 松开退出”。
- 手退出后继续使用原有 1360ms 泥核扩散。
- 以磁州窑的 `yaw`、`pitch` 和索引作为启动球面默认状态；动画结束后显示磁州窑窑火。
- 保留减少动态偏好回退：减少动态时跳过装饰动画并直接进入磁州窑。
- 在 1:1 iPhone 画布和 Pixel 10 画布完成浏览器验证，设计 QA 见 `design-qa.md`，结果为 `passed`。

## 交接验证

2026-08-02 在当前工作区完成：

- `npm.cmd run check:runtime`：通过，28 个受保护运行时文件完整。
- `npm.cmd run build`：通过，TypeScript 与 Vite 生产构建成功。
- `npm.cmd run test:sites`：通过，4 项 Sites Worker 测试全部成功。

从最新任务接管后再次执行以上三项校验，结果仍全部通过；Sites 构建产物完整。

项目当前不在 Git 仓库中，因此没有可供回滚或审阅的提交历史。继续开发前，建议先初始化版本控制或把目录纳入现有仓库。

## 2026-08-03 最新设计反馈与待办

来源任务：`019fc313-6dc7-7162-82ff-c8cfca0dc0a5`。

用户观看现有“两次轻捏泥核”启动动画后，认为它虽然有趣，但与进入窑境后的二级界面存在视觉与叙事割裂。当前动画应视为可运行原型，而不是已经定稿的首屏方向。

最新待办不是直接修改代码，而是先完成一份有史料依据的交互方案：

- 选择一件与当前磁州窑入口能够建立关联的宋代器物作为研究对象，优先研究梅瓶等轮制器物。
- 查清器物从备泥到成形的制作流程，尤其区分揉泥、上轮、定中心、开孔、提拉器壁等早期动作。
- 判断哪一个动作既符合真实制瓷逻辑，又能在纯黑首屏上被简洁辨认，并自然过渡到“窑境 → 窑火 → 器物”二级体验。
- 输出动作分镜、交互触发、泥团形变、镜头与材质衔接方案；用户确认后再替换当前动漫手两次轻捏实现。

上一任务仅开始检索故宫博物院、British Museum、The Met 及相关陶瓷成形资料，尚未形成研究结论或正式方案。接管后不得把该调研标记为已完成，也不应在用户确认新方案前删除当前可运行动画。

该调研现已完成，正式方案见 `design-audit/song-cizhou-meiping-intro-concept.md`。推荐方向为“定心开器”：泥团落在陶轮中心，双手扶泥定心，拇指从顶部开孔，镜头进入孔内并由拉坯旋纹过渡到磁州窑黑白窑境。该方案现已接入运行代码；旧版两次轻捏素材保留在 `public/assets/kilns/intro/`，仅作为可回退基线。

后续素材准备也已完成到可生成阶段：

- 中文艺术指导版提示词见 `design-audit/song-cizhou-meiping-intro-prompts-v2-zh.md`，包含风格母图、定心泥团、开孔泥团、左右手和孔内旋纹转场共 6 组提示词。
- 四张生成用原始参考图已整理到 `assets-source/intro-v2-reference/`，使用顺序与禁用旧版动漫手素材的原因见该目录 `README.md`。
- Prompt 01 已生成并归档为 `assets-source/intro-v2-reference/05-style-mother-v1.png`；它可作为后续泥团、手部与转场素材的统一光线、泥料和动作参照，但不直接作为运行时图层。
- Prompt 02 已生成并归档为 `assets-source/intro-v2-reference/06-clay-centered-v1.png`；它是新首屏“定心完成”状态，下一步仅在该图上编辑生成中心开孔与初始器壁。
- Prompt 03 已生成并归档为 `assets-source/intro-v2-reference/07-clay-opened-v1.png`；孔口小、居中且保留厚实泥壁，可进入左右手独立图层生成阶段。
- 左手扶泥素材已生成并归档为 `assets-source/intro-v2-reference/08-hand-left-support-green-v1.png`；它是纯绿背景原始层，运行前需要执行色键去背并与泥团左上外壁对齐。
- 右手开孔素材已生成并归档为 `assets-source/intro-v2-reference/09-hand-right-opening-green-v1.png`；它从母图提取而来，保留拇指向下的开孔手势，运行前同样需要色键去背并与 `07-clay-opened-v1.png` 的孔口对齐。
- Prompt 06 已生成并归档为 `assets-source/intro-v2-reference/10-cavity-spiral-transition-v1.png`；它以深褐湿泥内腔为中心，外圈自然转为磁州窑暖白与炭黑曲线。
- 两张绿幕手图已通过软边色键和去绿边预处理为透明 WebP；定心泥团、开孔泥团与转场纹理也已压缩，全部位于 `public/assets/kilns/intro-v2/`，合计约 635 KB。
- `src/Prototype.tsx` 与 `src/prototype.css` 已替换旧版“两次轻捏”状态机：触碰／Enter／Space 依次触发定心（1050 ms）、开孔（1000 ms）、孔内转场（1150 ms），再显示以磁州窑为中心的八窑球面。减少动态偏好仍会直接进入球面。
- 已在本地 iPhone 预览检查初始泥团、双手定心、开孔、孔内扩屏和磁州窑显现；生产构建、受保护运行时校验与 4 项 Sites Worker 测试均通过。
- 任务 `019fc7c0-d6b3-70c0-b390-478f750a3f77` 已完成新版首屏素材接入与验证；其任务状态后来显示为 `systemError`，但已落盘的代码、素材与构建产物完整存在。2026-08-04 由任务 `019fc922-bbf0-7cb0-8c8c-f482bde67390` 接管并重新执行 `check:runtime`、生产构建与 Sites 测试，三项均通过。`test:runtime` 的 8 项测试仍因本机缺少 Playwright Chromium 可执行文件而未进入测试逻辑，核心错误为 `Executable doesn't exist at C:\Users\15476\AppData\Local\ms-playwright\chromium_headless_shell-1228\chrome-headless-shell-win64\chrome-headless-shell.exe`；这属于测试环境依赖缺失，不是应用断言失败。两个任务共用 `D:/vbcoding/宋瓷/1/kiln-wheel-demo`，因此未复制第二份项目。

## 2026-08-04 本轮续接完成

本轮在保留“定心开器”既定方向、八窑球面、三窑窑火和移动端运行时的前提下，完成了以下工作：

- 首屏状态机增加 `opened` 开孔显露阶段：定心 1050ms、开孔 1000ms、开孔显露停留 220ms、孔内转场 1150ms；右手略抬起后再进入旋纹转场。
- 首屏启动前预热原始泥团、定心泥团、开孔泥团、双手和孔内旋纹素材，降低冷启动时首段仍显示原始泥团的概率。
- 球面揭示动画跨越 `opening`、`opened` 和 `entering` 阶段，避免阶段切换时出现突然跳亮或缩放回弹。
- 根目录 `design-qa.md` 已从旧版“两次轻捏泥核”更新为新版“定心开器”验收文档；新版 `qa/intro-v2-visual-qa-2026-08-04/QA_REPORT.md` 也已同步更新。
- `npm.cmd run check:runtime`、`npm.cmd run build`、`npm.cmd run test:sites` 均通过；本地浏览器已复核首屏阶段、磁州窑窑火、器物阅读和 Escape 返回，控制台无 error/warn。
- `npm.cmd run test:runtime` 仍因本机缺少 Playwright Chromium 可执行文件而未进入测试断言；该限制属于测试环境依赖，不是应用断言失败。

本轮未恢复旧版动漫手两次轻捏方案，也未修改受保护的移动端运行时文件。

## 2026-08-04 无手版首屏方向试验

根据最新设计反馈，首屏进一步收敛为“原始泥团 → 直接开孔 → 孔内旋纹转场 → 磁州窑二级界面”，暂时移除双手运行时动画，以减少视觉层次和材质冲突。

- `src/Prototype.tsx` 当前状态机为 `idle → opening → opened → entering → hidden`；不再包含 `centering` 状态。
- 当前运行时只使用原始泥团、开孔泥团和孔内旋纹三层素材；`clay-centered.webp` 与两张手部 WebP 保留但不再由首屏 DOM 引用。
- `opening` 持续 1000ms，`opened` 保持 220ms，`entering` 持续 1150ms；磁州窑球面、窑火入口、器物阅读和移动端运行时均保留。
- `design-qa.md` 与 `qa/intro-v2-visual-qa-2026-08-04/QA_REPORT.md` 已切换为无手版 QA 说明；旧版带手截图仅作为历史基线。
- 本轮代码改动后的 `check:runtime`、生产构建与 Sites 测试均通过；本地预览已复核无手版状态顺序、磁州窑球面、窑火进入、器物阅读和 Escape 返回，控制台无 error/warn。无手版 iPhone / Pixel 10 截图已归档到 `qa/no-hands-visual-qa-2026-08-04/`。

## 2026-08-04 旋转开孔泥坯验证收尾

来源任务最后停在“用现有开孔泥坯和孔内旋纹完成结构验证”。本轮已补完：

- iPhone 与 Pixel 10 均验证 `accelerating → entering → hidden`；
- 孔内旋纹接管到全屏，随后窑火入口、器物阅读均正常；
- 浏览器控制台无 `error` / `warn`；
- 结论为方向可用，但二维整图旋转和转场矩形边界不能直接作为最终视觉版本。

验证版只通过 `?introValidation=rotating` 打开，默认首页保持无手版。正式素材制作包、逐项提示词、参考图组合和验收标准见 `design-audit/rotating-clay-validation-and-asset-plan-2026-08-04.md`。

用户复核后进一步确认：验证版中的单张二维泥坯旋转视觉不通过，不能作为正式动画；现有孔内旋纹转场可以保留。后续应重新生成“稳定母图 + 慢速三维旋转循环 + 入孔终点帧 + 加速推镜视频”，再接回现有旋纹。最新正式计划、参考图打包和逐项提示词见 `design-audit/rotating-clay-generation-plan-v2-zh.md`；旧制作包仅作为验证历史。

## 来源

本说明最初从 Codex 任务 `019fc0c5-831a-7360-9945-e6840740e165` 整理，随后由任务 `019fc127-4665-7d42-a631-f54570181a07`、`019fc1bb-7996-72a0-986f-99c31a167189`、`019fc313-6dc7-7162-82ff-c8cfca0dc0a5`、`019fc6d9-e8b8-70a3-b249-0b9b2d397ccc` 与 `019fc7c0-d6b3-70c0-b390-478f750a3f77` 继续；现由任务 `019fc922-bbf0-7cb0-8c8c-f482bde67390` 接管。相关任务与当前任务使用同一工作目录，因此无需复制代码；本文件已更新为当前任务的接管基线。
